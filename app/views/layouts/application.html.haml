!!!
%html
  %head
    %title Todo
    = stylesheet_link_tag    "application", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body  
    .row-fluid
      .span10.offset1
        .hero-unit.text-center
          %h1
            ToDo 
          %p
            Welcome to the tutorial's ToDo application
          .text-center
            = link_to 'New task', new_task_path, remote: true, class:  'btn btn-primary input-large'
          .text-center
            - # %strong= current_user.email
          .text-center
            - # = link_to 'logout', destroy_user_session_path, method: :delete
          %a{ href: "http://localhost:3000/report", class: "btn btn-primary", target: "_blank" } REACT
    = yield
